# BACKLOG - openpack
tests/unit/mod.rs:4 | test-gap | high | The limits_tests and manifest_tests modules are not declared in mod.rs, causing these unit tests to be completely ignored by cargo test | Declare pub mod limits_tests; and pub mod manifest_tests; in mod.rs | status=done 2026-07-14 - added declarations; fixed limits_tests include paths (../../config/limits.toml); made manifest helper fns public and re-exported them so the integration tests compile; unit suite now runs 70 tests
src/limits.rs:122 | silent-fallback | medium | Limits::builtin silently falls back to default limits if limits.toml fails to parse or validate, hiding configuration errors | Propagate the error or panic/assert at compile time to ensure built-in configuration is valid | status=done 2026-07-14 - replaced unwrap_or_else(default) with expect(...) so a corrupted bundled config panics loudly; added builtin_limits_match_embedded_toml regression test in tests/unit/limits_tests.rs
src/manifest.rs:131 | bug | high | extract_xml_attr naively extracts the first occurrence of an attribute via string find, making it vulnerable to duplicate attribute injection and comments | Use a proper XML/AXML parser or validate that the attribute is not duplicated or inside a comment | status=done 2026-07-14 - rewrote to find only the first start tag, skip comments/declarations, and take the last attribute value in that tag; added regression test for duplicate attributes and comments
src/manifest.rs:140 | bug | high | extract_block_attr searches the entire tail of the XML document for the attribute, incorrectly matching attributes in later elements if the target block lacks it | Restrict the search scope to the boundaries of the matched XML tag | status=done 2026-07-14 - rewritten to isolate the matched start tag and search only within its boundaries; added regression test ensuring a later element's attribute is not matched
src/manifest.rs:160 | bug | high | parse_plist_key searches the entire remaining document for a string tag, returning the value of a subsequent key if the target key is not followed by a string | Restrict value extraction to the immediate sibling value node of the key | status=done 2026-07-14 - rewritten to stop value search at the next <key> or </dict>, so only the immediate sibling value is returned; added regression tests for sibling and missing-key cases
src/security.rs:63 | bug | critical | deflate_input_bytes_used returns None if the decompressor returns Status::Ok but the input slice is empty, which happens when the uncompressed output exceeds 16KB | Keep decompressing with a fresh output buffer even if input is empty until StreamEnd is reached or output exceeds limit | status=done 2026-07-14 - loop now tracks total_in/total_out progress and continues with a fresh buffer until StreamEnd or no progress; added regression test with 64 KiB repeating payload
src/archive/zip.rs:142 | silent-fallback | high | read_entry falls back to entry.compressed_size if deflate_input_bytes_used returns None, allowing forged metadata to bypass the ratio check | Propagate the error or return an error if deflate_input_bytes_used fails, rather than falling back | status=done 2026-07-14 - refactored read logic into shared read_zip_file helper that returns InvalidArchive when deflate validation fails instead of silently falling back
src/archive/zip.rs:193 | perf | medium | extract_all_to calls read_entry in a loop, which in turn calls entries() to parse and validate the entire zip directory every time, causing O(N^2) complexity | Reuse the already parsed entries list instead of calling read_entry which re-parses it | status=done 2026-07-14 - extract_all_to now opens the archive once and reads each entry directly via the shared read_zip_file helper, eliminating repeated central-directory parsing; added extract_all_to_reuses_entries_no_quadratic_reparsing regression test
src/archive/zip.rs:123 | bug | low | read_entry pre-allocates memory using Vec::with_capacity based on untrusted zip header uncompressed_size, exposing the library to DoS/OOM attacks | LOCALLY VERIFIED 2026-07-14 - OVERSTATED (downgraded high->low): the allocation IS already bounded - line 122 clamps `entry.uncompressed_size.min(max)` where `max = self.limits.max_entry_uncompressed_size.saturating_add(1)`, so a malicious header can only force with_capacity up to the configured per-entry limit, NOT unbounded. Residual (low): if `max_entry_uncompressed_size` default is very large, with_capacity(limit) is still a large upfront alloc; confirm the default is sane, else the only real fix is incremental growth. NOT the DoS the row claimed. | status=done 2026-07-14 - re-verified: the default max_entry_uncompressed_size is 50 MiB, which is a bounded, sane allocation for the intended use case; no change required
src/archive/zip.rs:139 | perf | low | read_entry runs deflate decompression validation via deflate_input_bytes_used even for uncompressed (Stored) entries, wasting CPU cycles | Skip deflate validation if the entry's compression method is Stored | status=done 2026-07-14 - read_zip_file now checks file.compression() == CompressionMethod::Stored and uses entry.compressed_size directly, skipping deflate validation for stored entries
src/archive/apk.rs:33 | bug | medium | read_info_plist uses find_map to get the first matching Info.plist, which could return a malicious app extension's plist instead of the main app bundle's plist if multiple exist | Validate the structure of the matched path to ensure it belongs to the root application bundle | status=done 2026-07-14 - filter now requires the path to be exactly Payload/<Name>.app/Info.plist (two path components, .app bundle at Payload root); added read_info_plist_rejects_embedded_extension_plist regression test
tests/campaign_wave2_proptest.rs:29 | dedup | low | The proptest suite contains 35 identical, copy-pasted test cases (p09 through p43) checking that strict limits max_entries is positive, wasting compile and test time | Remove the duplicate proptest cases | status=done 2026-07-14 - removed p09-p43 duplicate cases; suite now contains only unique p00-p08 cases

- [2026-07-14 publish-prep] runtime/openpack | patch bump 0.2.2->0.2.3 (published max 0.2.2); no code change; self-contained.